# GPU training stack (RTX 3080 Ti, 12 GB). Separate from the sim's
# requirements.txt on purpose — the game never needs any of this.
#
# torch needs the CUDA build; install it FIRST with the matching index, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then:
#   pip install -r train/requirements-train.txt
#
# bitsandbytes on native Windows can fail to load; train_lora.py and
# TransformersProvider both fall back to fp16 automatically (or run under WSL).
transformers>=4.44
datasets>=2.20
peft>=0.11
trl>=0.20
bitsandbytes>=0.43
accelerate>=0.30
